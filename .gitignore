# Python
__pycache__/
*.py[cod]
*.egg-info/
*.pdf
.venv/
venv/

# Eval run outputs (large, regenerable) — unanchored: matches runs/ at any depth
runs/

# Downloaded dataset (StrawDI_Db1 — re-downloadable, ~139 MB)
blog_study/strawdi_eval/data/

# Model weights (large binaries)
small_detector/weights/
*.pt

# Agent scratch dirs — harness creates them, must stay empty
blog_study/vlm_eval/agent_cwd/
blog_study/strawdi_eval/agent_cwd/

# Local tool settings
.claude/settings.local.json

# OS / editor junk
.DS_Store
*.swp

# Agent-bridge runtime state (regenerable)
blog_study/strawdi_eval/agent_bridge/state/

# paper_study: derived testbed frames + GT checks (datasets live on the mount)
paper_study/data/
paper_study/agent_cwd/
# temporary bigger testbed manifests (regenerable: build_testbed_manifest.py --per-source N)
paper_study/manifest[0-9]*.json
