{
  "type": "object",
  "additionalProperties": false,
  "required": ["fruits"],
  "properties": {
    "fruits": {
      "type": "array",
      "description": "EVERY fruit instance visible in the image, ripe or not, including partially occluded ones.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["bbox", "polygon", "confidence_pct"],
        "properties": {
          "bbox": {
            "type": "array",
            "minItems": 4,
            "maxItems": 4,
            "description": "Bounding box [x1, y1, x2, y2] of the whole fruit, including the parts hidden behind leaves or other fruit. Fruit body only: exclude the calyx and stem when they sit apart from the fruit; a calyx lying flat on the fruit is inside.",
            "items": {"type": "integer"}
          },
          "polygon": {
            "type": "array",
            "minItems": 3,
            "maxItems": 32,
            "description": "Ordered outline of the fruit's VISIBLE surface as [x, y] pixel vertices, walked once around the boundary (clockwise or counter-clockwise, start anywhere). Follow occluder edges where the fruit is hidden - do not extrapolate the hidden shape. 8-20 vertices for a typical fruit, 32 at most; simple closed outline, no repeated closing vertex.",
            "items": {
              "type": "array",
              "minItems": 2,
              "maxItems": 2,
              "items": {"type": "integer"}
            }
          },
          "confidence_pct": {
            "type": "integer",
            "minimum": 0,
            "maximum": 100,
            "description": "How confident you are that this really is a fruit instance of the asked kind."
          }
        }
      }
    }
  }
}
